// Main entry point - imports and initializes the app

import { app } from './js/app.js';

// The app initializes itself
console.log('Vodevs Studio loaded');

// Export for debugging
window.__vodevs = { app };
